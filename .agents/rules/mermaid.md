# Mermaid Flowchart Layout Rule

All Mermaid flowcharts generated for slides or documents in this repository should prioritize a left-to-right layout (`graph LR` or `flowchart LR`) rather than top-down (`TD` or `TB`) layouts. This makes them easier to fit and display horizontally on landscape slide layouts.
